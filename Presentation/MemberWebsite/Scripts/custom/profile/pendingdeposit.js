// Generated by IcedCoffeeScript 108.0.9
(function() {
  var __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  this.PendingDeposit = (function(_super) {
    __extends(PendingDeposit, _super);

    function PendingDeposit(id) {
      this.id = id;
      PendingDeposit.__super__.constructor.apply(this, arguments);
      this.fullName = ko.observable('test').extend({
        validatable: true
      });
      this.idFront = ko.observable('').extend({
        validatable: true
      });
      this.idBack = ko.observable('').extend({
        validatable: true
      });
      this.transferMethod = ko.observable('').extend({
        validatable: true
      });
      this.transferBank = ko.observable('').extend({
        validatable: true
      });
      this.depositReceipt = ko.observable('').extend({
        validatable: true
      });
      this.depositAmount = ko.observable('').extend({
        validatable: true
      });
      this.remark = ko.observable('').extend({
        validatable: true
      });
      this.submitDepositConfirmation = function() {
        return alert("confirm deposit");
      };
    }

    return PendingDeposit;

  })(FormBase);

}).call(this);
