// Generated by IcedCoffeeScript 108.0.9
(function() {
  define(function(reguire) {
    var DialogViewModel, config, dialog, i18N;
    dialog = require("plugins/dialog");
    i18N = require("i18next");
    config = require("config");
    return DialogViewModel = (function() {
      function DialogViewModel(data) {
        this.data = data;
        this.applySanctions = ko.observable('0');
        this.newSanction = ko.observable('0');
      }

      DialogViewModel.prototype.update = function() {
        this.data.action = this.applySanctions();
        this.data.sanction = this.newSanction();
        return $.post('/signup/update', {
          data: this.data
        }).done((function(_this) {
          return function(response) {
            _this.callback();
            return dialog.close(_this);
          };
        })(this));
      };

      DialogViewModel.prototype.cancel = function() {
        return dialog.close(this);
      };

      DialogViewModel.prototype.show = function(callback) {
        this.callback = callback;
        return dialog.show(this);
      };

      return DialogViewModel;

    })();
  });

}).call(this);
