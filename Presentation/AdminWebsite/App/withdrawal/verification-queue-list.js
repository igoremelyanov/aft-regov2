// Generated by IcedCoffeeScript 108.0.9
(function() {
  var __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  define(function(require) {
    var ViewModel, i18n, jgu, nav, security, withdrawalLogDialogViewModel;
    require("controls/grid");
    nav = require("nav");
    i18n = require("i18next");
    jgu = require("JqGridUtil");
    security = require("security/security");
    withdrawalLogDialogViewModel = require("withdrawal/withdrawal-log-dialog");
    return ViewModel = (function(_super) {
      __extends(ViewModel, _super);

      function ViewModel() {
        ViewModel.__super__.constructor.apply(this, arguments);
      }

      ViewModel.prototype.playerInfo = function(data, event) {
        var id;
        id = event.target.id;
        return nav.open({
          path: "player-manager/info",
          title: i18n.t("app:playerManager.list.playerInfo"),
          data: {
            playerId: id
          }
        });
      };

      ViewModel.prototype.avcStatus = function(data, event) {
        var dialog, id;
        id = event.target.id;
        dialog = new withdrawalLogDialogViewModel(id, "/OfflineWithdraw/AutoVerificationStatus");
        return dialog.show();
      };

      ViewModel.prototype.riskStatus = function(data, event) {
        var dialog, id;
        id = event.target.id;
        dialog = new withdrawalLogDialogViewModel(id, "/OfflineWithdraw/RiskProfileCheckStatus");
        return dialog.show();
      };

      ViewModel.prototype.riskTemplate = function(val) {
        if (val !== '-') {
          return val + '(<a data-id="@Id" style="color: #428bca" data-bind="click: $root.riskStatus" href="#">details</a>)';
        }
      };

      ViewModel.prototype.avcTemplate = function(val) {
        if (val !== '-') {
          return val + '(<a data-id="@Id" style="color: #428bca" data-bind="click: $root.avcStatus" href="#">details</a>)';
        }
        return val;
      };

      ViewModel.prototype.documents = function() {
        return nav.open({
          path: "withdrawal/withdrawal-verify",
          title: "Documents",
          data: {
            id: this.rowId(),
            event: "documents",
            gridId: "#withdraw-verify-grid"
          }
        });
      };

      ViewModel.prototype.investigate = function() {
        return nav.open({
          path: "withdrawal/withdrawal-verify",
          title: "Investigate",
          data: {
            id: this.rowId(),
            event: "investigate",
            gridId: "#withdraw-verify-grid"
          }
        });
      };

      ViewModel.prototype.view = function() {
        return nav.open({
          path: "withdrawal/withdrawal-verify",
          title: "View",
          data: {
            id: this.rowId(),
            event: "view",
            gridId: "#withdraw-verify-grid"
          }
        });
      };

      ViewModel.prototype.verify = function() {
        return nav.open({
          path: "withdrawal/withdrawal-verify",
          title: "Verify",
          data: {
            id: this.rowId(),
            event: "verify",
            gridId: "#withdraw-verify-grid"
          }
        });
      };

      ViewModel.prototype.cancel = function() {
        return nav.open({
          path: "withdrawal/withdrawal-verify",
          title: "Cancel",
          data: {
            id: this.rowId(),
            event: "cancel",
            gridId: "#withdraw-verify-grid"
          }
        });
      };

      ViewModel.prototype.unverify = function() {
        return nav.open({
          path: "withdrawal/withdrawal-verify",
          title: "Unverify",
          data: {
            id: this.rowId(),
            event: "unverify",
            gridId: "#withdraw-verify-grid"
          }
        });
      };

      return ViewModel;

    })(require("vmGrid"));
  });

}).call(this);
