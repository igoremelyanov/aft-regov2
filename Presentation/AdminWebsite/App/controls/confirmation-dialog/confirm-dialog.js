// Generated by IcedCoffeeScript 108.0.9
(function() {
  define(function(reguire) {
    var ConfirmDialog, dialog, i18n;
    dialog = require('plugins/dialog');
    i18n = require('i18next');
    return ConfirmDialog = (function() {
      function ConfirmDialog(settings) {
        this.caption = ko.observable('Caption');
        if (settings.caption) {
          this.caption(settings.caption);
        }
        this.yesCaption = ko.observable(i18n.t('common.yes'));
        if (settings.yesCaption) {
          this.yesCaption(settings.yesCaption);
        }
        this.noCaption = ko.observable(i18n.t('common.no'));
        if (settings.noCaption) {
          this.noCaption(settings.noCaption);
        }
        this.yesAction = function() {
          return {};
        };
        if (settings.yesAction) {
          this.yesAction = settings.yesAction;
        }
        this.noAction = function() {
          return {};
        };
        if (settings.noAction) {
          this.noAction = settings.noAction;
        }
        this.question = ko.observable('Question?');
        if (settings.question) {
          this.question(settings.question);
        }
      }

      ConfirmDialog.prototype.show = function() {
        return dialog.show(this);
      };

      ConfirmDialog.prototype.noClick = function() {
        if (this.noAction) {
          this.noAction();
        }
        return dialog.close(this);
      };

      ConfirmDialog.prototype.yesClick = function() {
        if (this.yesAction) {
          this.yesAction();
        }
        return dialog.close(this);
      };

      return ConfirmDialog;

    })();
  });

}).call(this);
